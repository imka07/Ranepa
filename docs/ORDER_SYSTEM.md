# 𝗔й𝗡тема Основного Оправления Заказами

## 𝗲одержание

1. [Архитектура](#архитектура)
2. [Новые файлы и компоненты](#новые-файлы-и-компоненты)
3. [Композабли](#композабли)
4. [Юз-кейсы](#юз-кейсы)
5. [АПО](#апо)

## Архитектура

### Данные заказа

```typescript
interface Order {
  id: string                          // Уникальный айди заказа
  userId: string                      // ID пользователя
  userName: string                    // Имя пользователя из useAuth
  userEmail: string                   // Email пользователя
  workType: string                    // essay | coursework | diploma | presentation | ...
  subject: string                     // Предмет (Математика, Обж, ...)
  theme: string                       // Тема работы
  deadline: string                    // Дедлайн (ISO 8601)
  volume: string                      // Объём страниц
  comment: string                     // Комментарий
  name: string                        // Контактное имя заказчика
  contactType: 'phone' | 'telegram'   // Преференсия контакта
  phone: string                       // Номер телефона
  telegram: string                    // Телеграм @им
  file?: { name: string; url: string }// Приложенные файлы
  status: 'в работе' | 'решен' | 'отменен'
  sections: OrderSection[]            // Подэтапы работы
  createdAt: string                   // Время составления
  updatedAt: string                   // Последние изменения
  messages: Message[]                 // Чат менеджер - пользователь
}

interface OrderSection {
  id: 'plan' | 'chapter1' | 'chapter2' | 'presentation'
  name: string                        // Название раздела
  completed: boolean                  // Готов ли
}

interface Message {
  id: string
  sender: 'user' | 'manager'
  text: string
  timestamp: string
}
```

## Новые файлы и компоненты

### Композабли

#### `composables/useOrders.ts`
Основное складисте тю для управления данными заказов. Хранит заказы в localStorage.

**Методы:**

```typescript
const { 
  orders,                           // Ref - все заказы
  createOrder,                      // (данные) => Order
  getUserOrders,                    // (userId) => Order[]
  getAllOrders,                     // () => Order[]
  addMessage,                       // (orderId, sender, text) => Message
  updateOrderStatus,                // (orderId, status) => void
  updateSectionStatus,              // (orderId, sectionId, completed) => void
  getOrder,                         // (orderId) => Order | undefined
  getOrderProgress                  // (orderId) => number
} = useOrders()
```

**Пример:**

```typescript
const { createOrder, getUserOrders, getOrderProgress } = useOrders()

// Сохранить новый заказ
const newOrder = createOrder({
  workType: 'coursework',
  subject: 'Математика',
  theme: 'Матрицы',
  deadline: '2026-02-01',
  volume: '10',
  comment: 'Вариант 5',
  name: 'Иван',
  contactType: 'phone',
  phone: '+7999999999',
  telegram: '@ivan'
})

// Получить заказы текущего пользователя
const myOrders = getUserOrders(user.value.id)

// Получить прогресс выполнения
const progress = getOrderProgress(orderId)  // 0-100
```

#### `composables/useOrderForm.ts`
Управляет формой для составления заказа.

**Методы:**

```typescript
const { 
  form,                   // Reactive - данные формы
  isOrderOpen,            // Ref - открыта ли модаль
  isLoading,              // Ref - отправляется ли
  alert,                  // Reactive - уведомление
  today,                  // Computed - сегодня
  canSubmit,              // Computed - можно ли отправить
  openOrderModal,         // () => void
  closeOrderModal,        // () => void
  submitOrder,            // async () => void
  resetForm               // () => void
} = useOrderForm()
```

### Компоненты

#### `components/OrderCard.vue`

Карточка для вывода заказа. Поддерживает репжим пользователя и админа.

**Props:**

```typescript
interface Props {
  order: Order
  isAdminView?: boolean      // Показывать админ кнопки
  showProgress?: boolean      // Показывать прогресс
  showSections?: boolean      // Показывать разделы
  showActions?: boolean       // Показывать кнопки действий
}
```

**Events:**

```typescript
emit('view-details', [])  // Открыть детали заказа
emit('update-status', status)  // Обновить статус
emit('toggle-section', sectionId)  // Отметить раздел выполненным
```

#### `components/OrderChat.vue`

Чат для коммуникации заказчика с менеджером.

**Props:**

```typescript
interface Props {
  orderId: string
  messages: Message[]
}
```

**Events:**

```typescript
emit('send-message', text)  // Отправить сообщение
```

### Новые страницы

#### `pages/dashboard.vue`

Обновленная Лочная кабинет пользователя с заказами.

**Фичеры:**
- Навигация на карточок Час заказ (заказы | отзывы | FAQ | настройки)
- Набвываемые Карточки Ордера для каждого заказа
- Конкретные заказы пользователя

#### `pages/admin/orders.vue`

Панель админа для управления всеми заказами.

**Фичеры:**
- Поиск и фильтрация всех заказов
- Обновление статуса
- Отметивание разделов выполненными
- Прогресс на каждом заказе

#### `pages/order/[id].vue`

Детальный выставок заказа с чатом.

**Фичеры:**
- Полная информация заказа
- Прогресс выполнения работы
- Чат с менеджером
- Отсчёт о оставшихся днях

## Композабли

## Юз-кейсы

### 1. Установка заказа

Пользователь:
1. Открывает главную страницу
2. Нажимает "Ордерю Пдание"
3. Она пополняет форму
4. Нажимает "Отправить"
5. Данные сохраняются в localStorage
6. На API сносится уведомление в TG бот
7. Пользователь редиректится в читате дашбоарда

### 2. Просмотр заказов

Пользователь:
1. Опкрывает `/dashboard`
2. Может видеть только свои заказы
3. Просматривает детали и прогресс
4. Открывает чат для дальнейшего взаимодействия

### 3. Админ-маненеджмент

Админ:
1. Открывает `/admin/orders`
2. Видит все заказы всех пользователей
3. Может фильтровать по статусу и типу
4. Обновляет статусы и разделы
5. Читает мессаджи в чате

## АПО

### localStorage Шема

```json
{
  "orders": [
    {
      "id": "abc123",
      "userId": "user-id-123",
      "userName": "Иван",
      "userEmail": "ivan@mail.ru",
      "workType": "coursework",
      "subject": "Математика",
      "theme": "Матрицы",
      "deadline": "2026-02-01T00:00:00Z",
      "volume": "10",
      "comment": "Вариант 5",
      "name": "Иван",
      "contactType": "phone",
      "phone": "+7999999999",
      "telegram": "@ivan",
      "status": "в работе",
      "sections": [
        { "id": "plan", "name": "План", "completed": true },
        { "id": "chapter1", "name": "1 глава", "completed": true },
        { "id": "chapter2", "name": "2 глава", "completed": false },
        { "id": "presentation", "name": "Преза", "completed": false }
      ],
      "createdAt": "2026-01-21T10:00:00Z",
      "updatedAt": "2026-01-21T11:00:00Z",
      "messages": [
        {
          "id": "msg1",
          "sender": "manager",
          "text": "Запрос получен. Менеджер с вами скоро свяжется.",
          "timestamp": "2026-01-21T10:00:00Z"
        }
      ]
    }
  ]
}
```

### АНЕВПО для ТГ

**POST /api/orders**

Отправляет уведомление в TG бот.

```bash
curl -X POST http://localhost:3000/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван",
    "workType": "coursework",
    "subject": "Математика",
    "theme": "Матрицы",
    "deadline": "2026-02-01",
    "volume": "10",
    "comment": "Вариант 5",
    "contactType": "phone",
    "phone": "+7999999999",
    "telegram": "@ivan"
  }'
```

## Отправка в а Развёрнута

В рамках данного обновления чтены в localStorage.

**Примеры расширения:**

- [ ] Firebase Realtime Database для персистенности
- [ ] WebSockets для реальных обновлений
- [ ] Нотификации э-мейла для тракинга
- [ ] Онлайн проверка на анстантные в астизци
