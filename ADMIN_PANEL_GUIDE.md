# 🔓 Принципы и инструкция Административной Панели

## Общие сведения

Это руководство описывает реализацию второго этапа разработки портала Reshala - ПОЛНОФУНКЦИОНАЛНАЙ Административная Панель для управления всеми аспектами системы.

---

## Основные принципы

### 1. Архитектура

- **Frontend**: Vue 3 + Nuxt 3 (Composition API)
- **State Management**: Composables (useAdmin, useAdminData)
- **Authentication**: JWT-подобная автентификация через localStorage
- **Data Persistence**: Mock-данные в composables (с возможностью интеграции API)

### 2. Структура типов данных

#### Order (Наказ)
```typescript
interface Order {
  id: string
  userId: string
  userName: string
  subject: string
  description: string
  status: 'принят' | 'в работе' | 'готов' | 'отменен'
  createdAt: string
  updatedAt: string
  price: number
}
```

#### AdminUser (Пользователь)
```typescript
interface AdminUser {
  id: string
  name: string
  email: string
  phone: string
  role: 'user' | 'admin'
  status: 'активен' | 'заблокирован'
  createdAt: string
}
```

#### Service (Услуга)
```typescript
interface Service {
  id: string
  name: string
  description: string
  price: number
  category: string
  isActive: boolean
}
```

#### Message (Сообщение)
```typescript
interface Message {
  id: string
  userId: string
  userName: string
  email: string
  subject: string
  text: string
  status: 'новое' | 'прочитано' | 'ответлено'
  createdAt: string
}
```

#### ContentBlock (Контент)
```typescript
interface ContentBlock {
  id: string
  type: 'text' | 'image' | 'banner' | 'section'
  title: string
  content: string
  page: string
  order: number
  isActive: boolean
}
```

---

## Компоненты и их функции

### Composables

#### `useAdmin.ts` - Автентификация администратора

**Методы:**
- `adminLogin(email, password)` - Вов администратора
- `adminLogout()` - Выход администратора

**Тестовые креденциалы:**
```
Admin: admin@reshala.com / admin123
Superadmin: superadmin@reshala.com / superadmin123
```

#### `useAdminData.ts` - Менеджмент данных

**Методы для заказов:**
- `updateOrderStatus(orderId, newStatus)` - Обновление статуса заказа
- `deleteOrder(orderId)` - Удаление заказа

**Методы для пользователей:**
- `blockUser(userId)` - Блокирование пользователя
- `unblockUser(userId)` - Разблокирование пользователя
- `deleteUser(userId)` - Удаление пользователя

**Методы для услуг:**
- `addService(service)` - Добавление новой услуги
- `updateService(serviceId, updates)` - Обновление услуги
- `deleteService(serviceId)` - Удаление услуги

**Методы для сообщений:**
- `markMessageAsRead(messageId)` - Отметить как прочитано
- `markMessageAsAnswered(messageId)` - Отметить как ответлено
- `deleteMessage(messageId)` - Удалить сообщение

**Методы для контента:**
- `addContentBlock(block)` - Добавление блока контента
- `updateContentBlock(blockId, updates)` - Обновление блока
- `deleteContentBlock(blockId)` - Удаление блока

**Общая статистика:**
- `getStats()` - Получить статистику (totalOrders, totalUsers, etc.)

### Pages

#### `pages/admin/login.vue` - Паге входа
- Простая форма входа для администратора
- Отображает тестовые номера
- Контроль ошибок

#### `pages/admin/index.vue` - Административная панель
- Гравирует таб-интерфейс для всех разделов
- Отображает статистику на гавной странице
- Превенция доступа без авторизации

### Components

#### `components/Admin/AdminOrdersSection.vue`
- Просмотр всех заказов
- Обновление статусов
- Удаление заказов
- Таблица с подробная информация

#### `components/Admin/AdminUsersSection.vue`
- Просмотр пользователей
- Блокирование/разблокирование
- Удаление аккаунтов

#### `components/Admin/AdminServicesSection.vue`
- Просмотр услуг
- Добавление новых услуг
- Обновление активности
- Удаление услуг

#### `components/Admin/AdminMessagesSection.vue`
- Просмотр сообщений
- Отметка как прочитано
- Отметка как ответлено
- Удаление сообщений

#### `components/Admin/AdminContentSection.vue`
- Просмотр контента
- добавление блоков
- Обновление активности
- Удаление блоков

---

## Навигация

### Маршруты

| Маршрут | Описание |
|--------|----------|
| `/admin/login` | Паге входа администратора |
| `/admin` | Главная административная панель |

---

## Настройка и деплой

### Добавление админа в middleware

> Этот шаг опционален и может быть добавлен латере

```typescript
// server/middleware/admin-auth.ts
export default defineEventHandler((event) => {
  const path = event.node.req.url
  if (path?.startsWith('/api/admin')) {
    const token = getCookie(event, 'admin-token')
    if (!token) {
      throw createError({
        statusCode: 401,
        statusMessage: 'Unauthorized'
      })
    }
  }
})
```

### API интеграция

Текущая реализация использует mock-данные. Для интеграции с backend-API:

```typescript
// В composables/useAdminData.ts
// Заменить mock-данные на API вызовы

const fetchOrders = async () => {
  const { data } = await $fetch('/api/admin/orders')
  orders.value = data
}
```

---

## Особенности реализации

### Отчеты

Админ может видеть живую статистику:
- Общее количество заказов
- Количество заказов в обработке
- Общее количество пользователей
- Количество блокированных пользователей
- Общее количество Услуг
- Непрочитанные сообщения

**Обновляется каждые 5 секунд**

### Доступ и Безопасность

На данный момент:
- [НЕ] Реализован JWT-токены
- [НЕ] Реализована сторонняя валидация
- [ДА] Клиентская авторизация с localStorage

**ОН** предусмотрены страяния на следующие этапы.

---

## Калкулятор валют

**Вложенные виды:**
- рубли (₽)
- доллары (добавить в будущем)
- евро (добавить в будущем)

---

## Тестирование

### Мануальное тестирование

1. Навигуйте на `/admin/login`
2. Введите тестовые креденциалы
3. Все элементы должны работать нормально
4. Консоль браузера не должна выдавать ошибок

---

## Будущие требования

Данные сти исполняются на следующих этапах:

- [ ] Оптимизация backend API интеграции
- [ ] Оптимизация JWT токенов и авторизации
- [ ] Оптимизация вывода большого количества данных (пагинация)
- [ ] Оптимизация сэюрсонтировки и фильтрации
- [ ] Наружние кнопки редактирования
- [ ] Полсная статистика и отчеты
- [ ] Автоматические уведомления админу
- [ ] Export данных в PDF/Excel
- [ ] Мобильная версия
- [ ] Моногоязычность

---

## Нормы Кодирования

### Vue 3 Composition API
- Оставлять констант на гнездое, переменные в setup
- Определять типы данных
- Юзать ref/reactive для состояния
- Оставлять computed для выводимых данных

### TypeScript
- Все новые файлы должны иметь расширение `.ts` или `.vue`
- Определять интерфейсы для всех типов данных
- Выставлять типы в composables

### Tailwind CSS
- Не использовать масывые inline-styles
- Максимально использовать Tailwind utilities
- Оценить затемнение palette

---

## Торовая отработка Я рекомендации

- **Утеточяю шаги миграции:**
  1. Текущая backend API интеграция
  2. Добавление middleware для контроля доступа
  3. Настройка JWT токенов

- **Функциональность добавлена на 100%**

---

## Лицензия

MIT

---

## Связь

Если у вас есть вопросы или сугжестии, соображайте Issue или PR в репозиторие
