# Настройка системы заявок

Это руководство описывает, как настроить систему для приема и отправки заявок в Telegram.

## Техническая архитектура

```
Клиент (Frontend)
  ↓
Форма заявки
  ↓
  POST /api/orders
  ↓
Nuxt Server API
  ↓
Telegram Bot API
  ↓
Телеграм админ группа
```

## Необходимые шаги

### 1. Понимаем технологию

Говорим два слово:

- **Client**: Vue.js в `pages/index.vue` рендерит форму (modal)
- **Composable** (`useOrderForm.ts`): управляет состоянием формы и валидацией
- **API Endpoint** (`server/api/orders.post.ts`): принимает данные, ничего не сохраняют
- **Telegram**: только отправляет в телеграм

### 2. Настроика Telegram бота

#### 2.1 Получить токен бота

1. Откройте Telegram и найдите **@BotFather**
2. Напишите `/start`
3. Напишите `/newbot`
4. Последуйте инструкциям - выберите имя бота
5. Получите токен в виде: `123456789:ABCdefGHijklmNOpqrsTuVWXyZ`

#### 2.2 Получить ID чата/группы

**Вариант 1: Плис вам с ботом**
1. Напишите боту ЛЮБОЕ сообщение
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<BOT_TOKEN>/getUpdates
   ```
3. Найдите `"chat":{"id":<YOUR_CHAT_ID>}`

**Вариант 2: Группа**
1. Найдите и добавьте бота в группу
2. Напишите сообщение
3. Читайте getUpdates - ID будет ОтРИЦАТЕЛЬНым (e.g., -1234567890)

### 3. Настроика Переменных Окружения

#### 3.1 На локальной машине

1. Копируйте `.env.example` в `.env.local`:
   ```bash
   cp .env.example .env.local
   ```

2. Откройте `.env.local` и заполните:
   ```env
   TELEGRAM_BOT_TOKEN=123456789:ABCdefGHijklmNOpqrsTuVWXyZ
   TELEGRAM_CHAT_ID=-1234567890
   ```

#### 3.2 На сервере (Напр.Нетлифи, Версел, ит.d.)

1. В настройках проекта добавьте Переменные Окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`

### 4. Протестировать бота

1. Запустите проект:
   ```bash
   npm run dev
   ```

2. Откройте http://localhost:3000

3. Нажмите кнопку "Заказать"

4. Заполните форму и отправьте

5. Откройте группу/чат Telegram - вы должны увидеть сообщение вида:

```
🎯 Новая заявка

Информация о работе:
📚 Тип работы: Реферат
📖 Предмет: История
✏️ Тема: Черкессы
⏰ Срок: 25 января 2026 г.
📄 Объём: 10 стр.

Контактные данные:
👤 Имя: Иван
📞 +7 900 000-00-00

Комментарий:
Добавить введение
```

## Файлы системы

### На клиенте
- **`composables/useOrderForm.ts`** - основной composable для управления формой
- **`pages/index.vue`** - скрипт и стили модального окна

### На сервере
- **`server/api/orders.post.ts`** - endpoint для нриема данных и отсылки в Telegram

## Поток данных

```
Узер наполняет форму
  ↓
При выборе файла, конвертируем в Base64 (сторона клиента)
  ↓
POST /api/orders {
  workType: "essay",
  subject: "History",
  theme: "...",
  deadline: "2026-01-25",
  volume: "10",
  comment: "...",
  name: "Ivan",
  contactType: "phone",
  phone: "+7 900 000-00-00",
  file: { base64: "...", name: "...", type: "...", size: 123456 }
}
  ↓
API распарсит данные и типирует сообщение
  ↓
Отправляем в Telegram:
1. Текст оформленный (HTML)
2. Файл (document)
```

## Около стоража

### Кде хранятся данные?

**Нехоранятся**. Ни в BD, ни на диске. Только отправляются в Telegram.

### Что далее?

1. Если нужна СОХРАНЕНОСТЬ: добавьте BD (опр Монго)
2. Если нужны ОТКЛОНЕННЫЕ сообщения: добавьте DB дд доаполните структуру

## Логика Отправки

1. Узер заполняет все обязательные поля
2. Кнопка нажать "OFF" используят
3. Посылаем POST со всеми данными
4. Сервер делает POST в Telegram API
5. Эст алерт о успехе/ошибке
6. Модаль закрывается

## Откладка

### Структура сообщения Telegram

```html
🎯 <b>Новая заявка</b>

<b>Информация о работе:</b>
📚 Тип работы: [value]
📖 Предмет: [value]
✏️ Тема: [value]
⏰ Срок: [value]
📄 Объём: [value] стр.

<b>Контактные данные:</b>
👤 Имя: [value]
📞 [phone/telegram]

[Обязательные комментарий]
```

## Настройки бота в BotFather (Optional)

1. `/setcommands` - добавить команды
2. `/setdescription` - описание бота
3. `/setdefault_admin_rights` - равты админа
